// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddappp.suiinscription.domain.certificate.hibernate;

import java.util.*;
import java.math.BigInteger;
import java.util.Date;
import org.dddappp.suiinscription.domain.*;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.dddappp.suiinscription.domain.certificate.*;
import org.dddappp.suiinscription.specialization.*;
import org.dddappp.suiinscription.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateCertificateStateRepository implements CertificateStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("Id", "InscriptionId", "InscriptionHash", "SlotNumber", "Round", "Inscriber", "InscriptionTimestamp", "Amount", "InscriptionNonce", "InscriptionContent", "Version", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public CertificateState get(String id, boolean nullAllowed) {
        CertificateState.SqlCertificateState state = (CertificateState.SqlCertificateState)getCurrentSession().get(AbstractCertificateState.SimpleCertificateState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractCertificateState.SimpleCertificateState();
            state.setId(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (CertificateState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{CertificateState.SqlCertificateState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(CertificateState state) {
        CertificateState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (CertificateState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(CertificateState detached) {
        CertificateState persistent = getCurrentSession().get(AbstractCertificateState.SimpleCertificateState.class, detached.getId());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().save(persistent);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(CertificateState persistent, CertificateState detached) {
        ((AbstractCertificateState) persistent).merge(detached);
    }

}

