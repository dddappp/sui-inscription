// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddappp.suiinscription.domain.slot.hibernate;

import java.util.*;
import java.math.BigInteger;
import java.util.Date;
import org.dddappp.suiinscription.domain.*;
import org.hibernate.Session;
import org.hibernate.Criteria;
//import org.hibernate.criterion.Order;
import org.hibernate.criterion.Criterion;
import org.hibernate.criterion.Projections;
import org.hibernate.SessionFactory;
import org.dddappp.suiinscription.domain.slot.*;
import org.dddappp.suiinscription.specialization.*;
import org.dddappp.suiinscription.specialization.hibernate.*;
import org.springframework.transaction.annotation.Transactional;

public class HibernateSlotStateRepository implements SlotStateRepository {
    private SessionFactory sessionFactory;

    public SessionFactory getSessionFactory() { return this.sessionFactory; }

    public void setSessionFactory(SessionFactory sessionFactory) { this.sessionFactory = sessionFactory; }

    protected Session getCurrentSession() {
        return this.sessionFactory.getCurrentSession();
    }
    
    private static final Set<String> readOnlyPropertyPascalCaseNames = new HashSet<String>(Arrays.asList("SlotNumber", "GenesisTimestamp", "SlotMaxAmount", "MintedAmount", "Round", "QualifiedRound", "QualifiedInscriptionId", "QualifiedHash", "QualifiedTimestamp", "QualifiedDifference", "CandidateInscriptionId", "CandidateHash", "CandidateInscriber", "CandidateTimestamp", "CandidateAmount", "CandidateNonce", "CandidateDifference", "CandidateContent", "Version", "OffChainVersion", "CreatedBy", "CreatedAt", "UpdatedBy", "UpdatedAt", "Active", "Deleted"));
    
    private ReadOnlyProxyGenerator readOnlyProxyGenerator;
    
    public ReadOnlyProxyGenerator getReadOnlyProxyGenerator() {
        return readOnlyProxyGenerator;
    }

    public void setReadOnlyProxyGenerator(ReadOnlyProxyGenerator readOnlyProxyGenerator) {
        this.readOnlyProxyGenerator = readOnlyProxyGenerator;
    }

    @Transactional(readOnly = true)
    public SlotState get(Integer id, boolean nullAllowed) {
        SlotState.SqlSlotState state = (SlotState.SqlSlotState)getCurrentSession().get(AbstractSlotState.SimpleSlotState.class, id);
        if (!nullAllowed && state == null) {
            state = new AbstractSlotState.SimpleSlotState();
            state.setSlotNumber(id);
        }
        if (getReadOnlyProxyGenerator() != null && state != null) {
            return (SlotState) getReadOnlyProxyGenerator().createProxy(state, new Class[]{SlotState.SqlSlotState.class}, "getStateReadOnly", readOnlyPropertyPascalCaseNames);
        }
        return state;
    }

    public void save(SlotState state) {
        SlotState s = state;
        if (getReadOnlyProxyGenerator() != null) {
            s = (SlotState) getReadOnlyProxyGenerator().getTarget(state);
        }
        if(s.getOffChainVersion() == null) {
            getCurrentSession().save(s);
        } else {
            getCurrentSession().update(s);
        }

        if (s instanceof Saveable)
        {
            Saveable saveable = (Saveable) s;
            saveable.save();
        }
        getCurrentSession().flush();
    }

    public void merge(SlotState detached) {
        SlotState persistent = getCurrentSession().get(AbstractSlotState.SimpleSlotState.class, detached.getSlotNumber());
        if (persistent != null) {
            merge(persistent, detached);
            getCurrentSession().save(persistent);
        } else {
            getCurrentSession().save(detached);
        }
        getCurrentSession().flush();
    }

    private void merge(SlotState persistent, SlotState detached) {
        ((AbstractSlotState) persistent).merge(detached);
    }

}

