// <autogenerated>
//   This file was generated by dddappp code generator.
//   Any changes made to this file manually will be lost next time the file is regenerated.
// </autogenerated>

package org.dddappp.suiinscription.restful.resource;

import java.util.*;
import java.util.stream.*;
import javax.servlet.http.*;
import javax.validation.constraints.*;
import org.springframework.http.MediaType;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.*;
import org.springframework.transaction.annotation.Transactional;
import org.dddml.support.criterion.*;
import java.math.BigInteger;
import java.util.Date;
import org.dddappp.suiinscription.domain.*;
import org.dddappp.suiinscription.specialization.*;
import org.dddappp.suiinscription.domain.slot.*;
import static org.dddappp.suiinscription.domain.meta.M.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.springframework.beans.factory.annotation.Autowired;
import org.dddml.support.criterion.TypeConverter;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@RequestMapping(path = "Slots", produces = MediaType.APPLICATION_JSON_VALUE)
@RestController
public class SlotResource {
    private Logger logger = LoggerFactory.getLogger(this.getClass());


    @Autowired
    private SlotApplicationService slotApplicationService;


    /**
     * Retrieve.
     * Retrieve Slots
     */
    @GetMapping
    @Transactional(readOnly = true)
    public SlotStateDto[] getAll( HttpServletRequest request,
                    @RequestParam(value = "sort", required = false) String sort,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "firstResult", defaultValue = "0") Integer firstResult,
                    @RequestParam(value = "maxResults", defaultValue = "2147483647") Integer maxResults,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
        if (firstResult < 0) { firstResult = 0; }
        if (maxResults == null || maxResults < 1) { maxResults = Integer.MAX_VALUE; }

            Iterable<SlotState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> SlotResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (SlotMetadata.aliasMap.containsKey(n) ? SlotMetadata.aliasMap.get(n) : n));
            states = slotApplicationService.get(
                c,
                SlotResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);

            SlotStateDto.DtoConverter dtoConverter = new SlotStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toSlotStateDtoArray(states);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve in pages.
     * Retrieve Slots in pages.
     */
    @GetMapping("_page")
    @Transactional(readOnly = true)
    public Page<SlotStateDto> getPage( HttpServletRequest request,
                    @RequestParam(value = "fields", required = false) String fields,
                    @RequestParam(value = "page", defaultValue = "0") Integer page,
                    @RequestParam(value = "size", defaultValue = "20") Integer size,
                    @RequestParam(value = "filter", required = false) String filter) {
        try {
            Integer firstResult = (page == null ? 0 : page) * (size == null ? 20 : size);
            Integer maxResults = (size == null ? 20 : size);
            Iterable<SlotState> states = null; 
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap().entrySet().stream()
                    .filter(kv -> SlotResourceUtils.getFilterPropertyName(kv.getKey()) != null)
                    .collect(Collectors.toMap(kv -> kv.getKey(), kv -> kv.getValue())));
            }
            Criterion c = CriterionDto.toSubclass(criterion, getCriterionTypeConverter(), getPropertyTypeResolver(), 
                n -> (SlotMetadata.aliasMap.containsKey(n) ? SlotMetadata.aliasMap.get(n) : n));
            states = slotApplicationService.get(
                c,
                SlotResourceUtils.getQuerySorts(request.getParameterMap()),
                firstResult, maxResults);
            long count = slotApplicationService.getCount(c);

            SlotStateDto.DtoConverter dtoConverter = new SlotStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            Page.PageImpl<SlotStateDto> statePage =  new Page.PageImpl<>(dtoConverter.toSlotStateDtoList(states), count);
            statePage.setSize(size);
            statePage.setNumber(page);
            return statePage;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    /**
     * Retrieve.
     * Retrieves Slot with the specified ID.
     */
    @GetMapping("{slotNumber}")
    @Transactional(readOnly = true)
    public SlotStateDto get(@PathVariable("slotNumber") Integer slotNumber, @RequestParam(value = "fields", required = false) String fields) {
        try {
            Integer idObj = slotNumber;
            SlotState state = slotApplicationService.get(idObj);
            if (state == null) { return null; }

            SlotStateDto.DtoConverter dtoConverter = new SlotStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toSlotStateDto(state);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_count")
    @Transactional(readOnly = true)
    public long getCount( HttpServletRequest request,
                         @RequestParam(value = "filter", required = false) String filter) {
        try {
            long count = 0;
            CriterionDto criterion = null;
            if (!StringHelper.isNullOrEmpty(filter)) {
                criterion = new ObjectMapper().readValue(filter, CriterionDto.class);
            } else {
                criterion = QueryParamUtils.getQueryCriterionDto(request.getParameterMap());
            }
            Criterion c = CriterionDto.toSubclass(criterion,
                getCriterionTypeConverter(), 
                getPropertyTypeResolver(), 
                n -> (SlotMetadata.aliasMap.containsKey(n) ? SlotMetadata.aliasMap.get(n) : n));
            count = slotApplicationService.getCount(c);
            return count;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{slotNumber}/_commands/Create")
    public void create(@PathVariable("slotNumber") Integer slotNumber, @RequestBody SlotCommands.Create content) {
        try {

            SlotCommands.Create cmd = content;//.toCreate();
            Integer idObj = slotNumber;
            if (cmd.getSlotNumber() == null) {
                cmd.setSlotNumber(idObj);
            } else if (!cmd.getSlotNumber().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", slotNumber, cmd.getSlotNumber());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            slotApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{slotNumber}/_commands/PutUpCandidate")
    public void putUpCandidate(@PathVariable("slotNumber") Integer slotNumber, @RequestBody SlotCommands.PutUpCandidate content) {
        try {

            SlotCommands.PutUpCandidate cmd = content;//.toPutUpCandidate();
            Integer idObj = slotNumber;
            if (cmd.getSlotNumber() == null) {
                cmd.setSlotNumber(idObj);
            } else if (!cmd.getSlotNumber().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", slotNumber, cmd.getSlotNumber());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            slotApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }


    @PutMapping("{slotNumber}/_commands/Advance")
    public void advance(@PathVariable("slotNumber") Integer slotNumber, @RequestBody SlotCommands.Advance content) {
        try {

            SlotCommands.Advance cmd = content;//.toAdvance();
            Integer idObj = slotNumber;
            if (cmd.getSlotNumber() == null) {
                cmd.setSlotNumber(idObj);
            } else if (!cmd.getSlotNumber().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", slotNumber, cmd.getSlotNumber());
            }
            cmd.setRequesterId(SecurityContextUtil.getRequesterId());
            slotApplicationService.when(cmd);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("_metadata/filteringFields")
    public List<PropertyMetadataDto> getMetadataFilteringFields() {
        try {

            List<PropertyMetadataDto> filtering = new ArrayList<>();
            SlotMetadata.propertyTypeMap.forEach((key, value) -> {
                filtering.add(new PropertyMetadataDto(key, value, true));
            });
            return filtering;

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{slotNumber}/_events/{version}")
    @Transactional(readOnly = true)
    public SlotEvent getEvent(@PathVariable("slotNumber") Integer slotNumber, @PathVariable("version") long version) {
        try {

            Integer idObj = slotNumber;
            //SlotStateEventDtoConverter dtoConverter = getSlotStateEventDtoConverter();
            return slotApplicationService.getEvent(idObj, version);

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }

    @GetMapping("{slotNumber}/_historyStates/{version}")
    @Transactional(readOnly = true)
    public SlotStateDto getHistoryState(@PathVariable("slotNumber") Integer slotNumber, @PathVariable("version") long version, @RequestParam(value = "fields", required = false) String fields) {
        try {

            Integer idObj = slotNumber;
            SlotStateDto.DtoConverter dtoConverter = new SlotStateDto.DtoConverter();
            if (StringHelper.isNullOrEmpty(fields)) {
                dtoConverter.setAllFieldsReturned(true);
            } else {
                dtoConverter.setReturnedFieldsString(fields);
            }
            return dtoConverter.toSlotStateDto(slotApplicationService.getHistoryState(idObj, version));

        } catch (Exception ex) { logger.info(ex.getMessage(), ex); throw DomainErrorUtils.convertException(ex); }
    }



    //protected  SlotStateEventDtoConverter getSlotStateEventDtoConverter() {
    //    return new SlotStateEventDtoConverter();
    //}

    protected TypeConverter getCriterionTypeConverter() {
        return new DefaultTypeConverter();
    }

    protected PropertyTypeResolver getPropertyTypeResolver() {
        return new PropertyTypeResolver() {
            @Override
            public Class resolveTypeByPropertyName(String propertyName) {
                return SlotResourceUtils.getFilterPropertyType(propertyName);
            }
        };
    }

    // ////////////////////////////////
 
    public static class SlotResourceUtils {

        public static void setNullIdOrThrowOnInconsistentIds(Integer slotNumber, SlotCommand value) {
            Integer idObj = slotNumber;
            if (value.getSlotNumber() == null) {
                value.setSlotNumber(idObj);
            } else if (!value.getSlotNumber().equals(idObj)) {
                throw DomainError.named("inconsistentId", "Argument Id %1$s NOT equals body Id %2$s", slotNumber, value.getSlotNumber());
            }
        }
    
        public static List<String> getQueryOrders(String str, String separator) {
            return QueryParamUtils.getQueryOrders(str, separator, SlotMetadata.aliasMap);
        }

        public static List<String> getQuerySorts(Map<String, String[]> queryNameValuePairs) {
            String[] values = queryNameValuePairs.get("sort");
            return QueryParamUtils.getQuerySorts(values, SlotMetadata.aliasMap);
        }

        public static String getFilterPropertyName(String fieldName) {
            if ("sort".equalsIgnoreCase(fieldName)
                    || "firstResult".equalsIgnoreCase(fieldName)
                    || "maxResults".equalsIgnoreCase(fieldName)
                    || "fields".equalsIgnoreCase(fieldName)) {
                return null;
            }
            if (SlotMetadata.aliasMap.containsKey(fieldName)) {
                return SlotMetadata.aliasMap.get(fieldName);
            }
            return null;
        }

        public static Class getFilterPropertyType(String propertyName) {
            if (SlotMetadata.propertyTypeMap.containsKey(propertyName)) {
                String propertyType = SlotMetadata.propertyTypeMap.get(propertyName);
                if (!StringHelper.isNullOrEmpty(propertyType)) {
                    if (BoundedContextMetadata.CLASS_MAP.containsKey(propertyType)) {
                        return BoundedContextMetadata.CLASS_MAP.get(propertyType);
                    }
                }
            }
            return String.class;
        }

        public static Iterable<Map.Entry<String, Object>> getQueryFilterMap(Map<String, String[]> queryNameValuePairs) {
            Map<String, Object> filter = new HashMap<>();
            queryNameValuePairs.forEach((key, values) -> {
                if (values.length > 0) {
                    String pName = getFilterPropertyName(key);
                    if (!StringHelper.isNullOrEmpty(pName)) {
                        Class pClass = getFilterPropertyType(pName);
                        filter.put(pName, ApplicationContext.current.getTypeConverter().convertFromString(pClass, values[0]));
                    }
                }
            });
            return filter.entrySet();
        }

        public static SlotStateDto[] toSlotStateDtoArray(Iterable<Integer> ids) {
            List<SlotStateDto> states = new ArrayList<>();
            ids.forEach(i -> {
                SlotStateDto dto = new SlotStateDto();
                dto.setSlotNumber(i);
                states.add(dto);
            });
            return states.toArray(new SlotStateDto[0]);
        }

    }

}

